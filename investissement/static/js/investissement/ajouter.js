$(document).ready(function () {

    // dates = $("input[type='date']")
    // jQuery.each(dates, function (i, input) {
    //     // $(input).val($(input).val().split('/').join('-'));
    //     console.log("###", $(input).val());
    // });
});